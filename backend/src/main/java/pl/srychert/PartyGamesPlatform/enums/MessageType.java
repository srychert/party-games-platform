package pl.srychert.PartyGamesPlatform.enums;

public enum MessageType {
    ANSWERS,
    RESULT,
    MESSAGE,
    CONNECT,
    DISCONNECT,
    START_GAME,

    JOIN,
    JOINED,
    CREATE_ROOM,
    DUPLICATE_NICK,
}
