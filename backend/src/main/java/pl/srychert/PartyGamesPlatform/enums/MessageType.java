package pl.srychert.PartyGamesPlatform.enums;

public enum MessageType {
    CREATE_ROOM, CREATED,
    START_GAME, STARTED,
    JOIN, JOINED,
    END_GAME, ENDED,
    DUPLICATE_NICK,
    NO_PIN, NO_ROOM,
    NEXT_ROUND,
    PLAY,
    ANSWER,
    NO_GAME, ERROR,
    NODE_OPTION,
    CHOOSE_NODE
}
